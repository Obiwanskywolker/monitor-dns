
services:
  - type: web
    name: monitor-dns
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    region: oregon
    branch: main
    rootDir: monitor_dns_render
